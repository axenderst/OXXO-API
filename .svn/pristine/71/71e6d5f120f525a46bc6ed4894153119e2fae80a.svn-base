using Oxxo2.Controllers;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Oxxo2.Models
{
    public class ModelFactory
    {
        public jsonEntities.InventarioJson Create(jsonEntities.InventarioJson inventario)
        {
            string InventoryName = inventario.InventarioNombre,
	        string GumFol = inventario.FolioGoma,
            variable2 = inventario. .Software.Select(s => Create(s))
        };

        public HardwareModel Create(Hardware hardware)
        {
            return new HardwareModel()
            {
                HardwareId = hardware.Hardware.Id,
                HardwareName = hardware.HardwareName
            };
        }
    }
}
